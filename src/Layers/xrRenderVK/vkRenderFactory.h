#pragma once
#include "../xrRender/RenderFactory.h" // Include the abstract base engine interface

class cvkRenderFactory : public IRenderFactory
{
    // Implement or stub out pure virtual methods like CreateRenderDeviceRender(), CreateRender(), etc.
};
